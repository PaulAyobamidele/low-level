#include <stdio.h>

int main(){
    int i, n, j, max, min, arr[20];

    printf("\n Enter the length of your array: ");
    scanf("%d", &n);

    
    for(i = 0; i < n; i++){
        printf("\n Enter each of the calues of the array: ");
        scanf("%d", &arr[i]);
    };

    
}